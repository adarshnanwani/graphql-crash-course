import React from 'react';
import PropTypes from 'prop-types';

const BookList = (props) => {
  return (
    <div>
      <ul>
        <li>Book </li>
      </ul>
    </div>
  );
};

BookList.propTypes = {};

export default BookList;
